# thesis-replication
This repository was created to include a full replication code of the analyses performed in Stata, along with a readme file explaining the actions. 
Seperate folders are also included, one named data, which contains all datasets used in the analyses performed, and a file titled outputs, which includes all graphs and tables generated by the code in the do-file.
